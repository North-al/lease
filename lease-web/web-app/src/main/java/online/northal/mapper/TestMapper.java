package online.northal.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;

public interface TestMapper extends BaseMapper<Object> {
    // This interface can be used to define custom query methods if needed.
    // It extends BaseMapper which provides basic CRUD operations.
}
